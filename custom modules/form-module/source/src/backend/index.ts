import * as sdk from 'botpress/sdk'

const entryPoint: sdk.ModuleEntryPoint = {
  definition: {
    name: 'form-module',
    menuIcon: 'flag',
    menuText: 'Form Module',
    noInterface: true,
    fullName: 'Form Module'
  }
}

export default entryPoint
